---
title: D - 修復異常檢測測試
status: in-progress
assignee: 未指派
created: 2026-04-16
updated: 2026-04-16
---

## 基本信息
- **Type**: Bug Fix
- **Assignee**: 碼農 2 號
- **Parent**: T004
- **Priority**: high
- **Estimate**: 10分鐘
- **Dependencies**: T004-B

## 問題描述

`test_clean_all` 測試失敗，異常值 99999.0 沒有被正確檢測到。

### 失敗測試
```python
def test_clean_all(self):
    clean_sample = [
        {"timestamp": datetime(2023, 1, 1, tzinfo=timezone.utc), "price": 1800.0},
        {"timestamp": datetime(2023, 1, 1, tzinfo=timezone.utc), "price": 1800.0},  # duplicate
        {"timestamp": datetime(2023, 1, 3, tzinfo=timezone.utc), "price": 99999.0}, # anomaly
        {"timestamp": datetime(2023, 1, 4, tzinfo=timezone.utc), "price": 1900.0},
    ]
    cleaned, stats = self.cleaner.clean_all(clean_sample)
    assert stats["duplicates"]["duplicate_count"] == 1
    assert stats["anomalies"]["anomaly_count"] == 1  # ❌ assert 0 == 1
```

### 根本原因

數據樣本太少（只有 4 筆），導致 IQR 計算不準確：
- Q1 = 1800.0
- Q3 = 1900.0  
- IQR = 100.0
- Lower = 1800 - 1.5*100 = 1650
- Upper = 1900 + 1.5*100 = 2050
- 99999.0 確實 > 2050，應該被檢測到

問題可能在於：
1. 重複值移除後，樣本只剩 3 筆
2. 異常檢測邏輯執行順序問題

## 修復方案

### 方案 1：調整檢測邏輯
確保異常檢測在重複值移除後仍能正確運作。

### 方案 2：調整測試數據
增加測試數據量，讓 IQR 計算更準確。

## 執行步驟

1. **診斷問題**
   ```bash
   cd /Users/claw/Projects/gold-analysis/backend
   python3 -m pytest tests/test_cleaners.py::TestPriceCleaner::test_clean_all -v
   ```

2. **定位代碼**
   - 檢查 `cleaners/price_cleaner.py` 的 `clean_all` 方法
   - 檢查 `cleaners/outlier_detector.py` 的檢測邏輯

3. **修復問題**
   - 選擇最簡單有效的方案
   - 確保測試通過

4. **驗證**
   ```bash
   python3 -m pytest tests/test_cleaners.py -v
   ```

5. **Commit**
   ```bash
   git add .
   git commit -m "fix(data): resolve anomaly detection in clean_all (T004-D)"
   git push
   ```

## 完成條件
- [ ] 測試全部通過（9/9）
- [ ] 已 commit 並 push

## 注意事項
- 不要破壞已有的 8 個通過的測試
- 優先考慮最小改動
