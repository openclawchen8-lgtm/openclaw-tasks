---

id: T016
project: gold-analysis-core
title: 系統集成測試
assignee: 樂樂
priority: medium
status: done
created: 2026-04-07
updated: 2026-04-09
estimate: 2天
depends: [T002, T003, T004, T005, T006, T007, T008, T012]

---

## 描述
端到端集成測試，覆蓋 cleaner → validator → API → Agent → 分析工具 → 信號生成 全鏈路。

## 完成條件
- [x] 數據管道集成測試完成
- [x] FastAPI 應用集成測試完成
- [x] 分析工具集成測試完成
- [x] Agent + 分析工具集成測試完成
- [x] 端到端流程測試完成
- [x] DB 模型集成測試完成
- [x] 錯誤處理測試完成

## 產出
- **檔案**: `backend/tests/test_system_integration.py`
- **測試案例數**: 24 個 SI（SI-001 ~ SI-024）
- **執行結果**: 24 passed, 1 skipped (SI-019 支撐/阻力)
- **覆蓋範圍**:
  - 數據管道：cleaner → validator（SI-001~003）
  - FastAPI：routes / models / validators 導入（SI-004~008）
  - 分析工具：MA/RSI/MACD/BB/ATR 鏈式調用（SI-009~012）
  - Agent+工具：Coordinator 調用真實 AnalysisTools（SI-013~016）
  - 端到端：完整分析流程 run_mock_analysis（SI-017~019）
  - DB 模型：Decision / Portfolio / PriceData 結構（SI-020~021）
  - 錯誤處理：空請求 / None 輸入容錯（SI-022~023）
- **依賴模組**: cleaners, validators, api, agents, tools（跨模組）

## 發現的代碼問題
| 位置 | 問題 | 嚴重性 |
|------|------|--------|
| `price_validator.py` | 無 `validate_prices()` 方法，正確方法為 `validate()` | ⚠️ 介面不一致 |
| `market_validator.py` | 無 `validate_market_data()` 方法，正確方法為 `validate()` | ⚠️ 介面不一致 |
| `models/market_data.py` | 無 `MarketData` 類，正確為 `PriceData`/`HistoricalPriceData` | ⚠️ 命名錯誤 |

## 備註
驗證任務，由 樂樂 執行。SI-014 為關鍵測試：真實調用 `AnalysisTools.calculate_ma()` 於 Coordinator pipeline 中。
