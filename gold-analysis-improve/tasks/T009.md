---


status: done
title: "統一 SQLite 資料來源 + 台灣銀行 1 年歷史數據"
assignee: 寶寶
created: 2026-04-18
priority: high
tags: [gold-analysis, backend, data-source, bot-gold]
updated: 2026-04-18


---

## 任務目標

統一 `/analysis` 和 `/technicals` 的資料來源：
- 兩頁皆從 **SQLite** 讀取歷史數據
- SQLite 的資料從**台灣銀行官網**補充近一年黃金歷史報價
- `/analysis` → SQLite → RSI mock 計算
- `/technicals` → SQLite → TechnicalAnalysisAgent

**注意**：JSON 檔（`gold_price_history.json`）**不再作為資料來源**，統一走 SQLite。

## 重大發現（2026-04-18）

**台灣銀行官網有整年歷史**：`https://rate.bot.com.tw/gold/chart/year/TWD`
- 244 筆每日黃金存摺牌告價（買進/賣出，NTD/克）
- 日期：2025-04-21 ~ 2026-04-17（1年）
- **直接對應 `local_buy` / `local_sell` 欄位，無需匯率轉換**
- ✅ RSI(14)/MA(20)/MA(60)/MACD 全部足夠

## 實作步驟

### Step 1 ✅ 確認 SQLite table schema

`price_history` table 欄位確認完畢。gold 已有 250 筆（2025-04-21 起）。

### Step 2 ✅ 台灣銀行1年歷史（已完成）

```python
# URL: https://rate.bot.com.tw/gold/chart/year/TWD
# HTML 解析: <td>日期</td> <td>買進</td> <td>賣出</td>
# international_spot = (buy + sell) / 2
# is_daily_close = 1
```

✅ 已完成：244 筆新增，gold 總計 **250 筆**（2025-04-21 ~ 2026-04-17）。

### Step 3：修改 `/api/decisions/recommend`

- 取全部 gold 歷史（`ORDER BY timestamp ASC`）
- RSI(14) / MA(5) / MA(10) / MA(60) 從全量計算
- 使用 `local_buy`（銀行買進價）作價格基準

### Step 4：修改 `/api/technicals`

廢除 JSON，改讀 SQLite：
- `SELECT timestamp, local_buy, local_sell FROM price_history WHERE metal='gold' ORDER BY timestamp ASC`
- `price` = `local_buy`（銀行買進，技術指標常用價）
- 丟給 TechnicalAnalysisAgent 計算

### Step 5：清理 main.py

移除 `/api/technicals` 的 `gold_price_history.json` 讀取邏輯。

## 實作檢查清單

- [x] Step 1：確認 SQLite schema ✅（250 筆，2025-04-21 起）
- [x] Step 2：台灣銀行1年歷史 ✅（244 筆已寫入）
- [x] Step 3：修改 `/api/decisions/recommend` 取全量 SQLite ✅（RSI=72.6）
- [x] Step 4：修改 `/api/technicals` 從 SQLite 讀取 ✅（廢除 JSON）
- [x] Step 5：清理 `/api/seasonality` JSON 邏輯 ✅（一併改完）
- [x] 驗證：API 回傳有 RSI(14)/MA(20)/MA(60)/MACD 數值 ✅
- [x] Build 確認（frontend）
- [x] Commit + Push

## 備註

- `is_daily_close=1`：台灣銀行每天只公布一個收盤牌告
- 台灣銀行 URL 只顯示滾動1年，需每年更新一次
- JSON 檔（`gold_price_history.json`）廢除，不再作為資料來源
- **不需要 Yahoo Finance**，台灣銀行的 NTD/克牌告價就是技術指標的基準
