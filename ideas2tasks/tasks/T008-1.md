---
id: T008-1
project: ideas2tasks
title: lifecycle.py + executor.py 迁移重构
assignee: 碼農 1 號
priority: high
status: done
created: 2026-04-23
updated: 2026-04-23
parent: T008
---

# T008-1 - lifecycle.py + executor.py 迁移重构

## 背景
lifecycle.py 是状态机核心，cron 依赖它执行周期扫描。executor.py 是任务执行器。两者都使用 sys.path.insert 反模式和旧版常量。

## 待办清单

- [x] 创建 `ideas2tasks/ideas2tasks/lifecycle.py`
- [x] 移除 sys.path.insert，改用相对导入
- [x] 更新导入：`from .config import get_tasks_dir, get_ideas_dir`
- [x] 更新导入：`from .state_sync import ...`
- [x] 创建 `ideas2tasks/ideas2tasks/executor.py`
- [x] 移除 sys.path.insert，改用相对导入
- [x] 在 `__main__.py` 添加 `lifecycle` 和 `executor` 子命令
- [x] 验证：`python3 -m ideas2tasks lifecycle --help`
- [x] 验证：`python3 -m ideas2tasks executor --help`
- [x] 旧 scripts/ 文件保留向后兼容

## 技术细节

### lifecycle.py 迁移
```python
# 旧版
sys.path.insert(0, str(Path(__file__).parent))
from state_sync import TASKS_DIR, IDEAS_DIR

# 新版
from .config import get_tasks_dir, get_ideas_dir
from .state_sync import merge_classify_with_tasks_status
```

### executor.py 迁移
```python
# 旧版
from state_sync import TASKS_DIR, IDEAS_DIR

# 新版
from .config import get_tasks_dir, get_ideas_dir
from .state_sync import should_skip_task
```

## 验收标准
- [x] `python3 -m ideas2tasks lifecycle --help` 正常执行
- [x] `python3 -m ideas2tasks executor --help` 正常执行
- [x] 无 sys.path.insert 残留
- [x] 使用相对导入
- [x] 旧 scripts/ 文件仍能运行

## 关联任务
- **父任务**：T008
- **后续**：T008-2（基础模块迁移）

## 重构日志

### 2026-04-23 18:31
- 发现 T007 遗漏 8 个模块（非只有 executor）
- 拆分为三个子任务：T008-1（P0）、T008-2（P1）、T008-3（P2）

### 2026-04-23 18:50
- 完成 T008-1 迁移
- 创建 lifecycle.py 和 executor.py（使用相对导入）
- 更新 __main__.py CLI
- 验证通过
- Git：openclaw-skills `30dce58`
