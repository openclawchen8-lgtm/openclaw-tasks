---
id: T007
project: ideas2tasks
title: Python 環境規範重構
assignee: 碼農 1 號
priority: high
status: done
created: 2026-04-23
updated: 2026-04-23
---

# T007 - Python 環境規範重構

## 背景
依據 qclaw-env SKILL.md 的 Python 環境規範，ideas2tasks scripts 目錄存在多處不符合規範的問題。

## 發現的問題

### P0 - 必須修復
1. **缺少 pyproject.toml** — 無專案元數據聲明 ✅ 已修復
2. **硬編碼路徑** — TASKS_DIR / IDEAS_DIR 寫死在 state_sync.py，無法移植 ✅ 已修復

### P1 - 建議修復
3. **sys.path.insert 反模式** — 模組導入依賴 hack，應改用正式包結構 ✅ 已修復
4. **缺少 __init__.py** — scripts 不是正式 Python 包 ✅ 已修復

### P2 - 可選
5. **uv 整合** — 即使依賴為空，也應建立 uv 專案結構 ⚠️ 跳過（uv 未安裝）
6. **備份檔殘留** — executor.py.bak 應刪除 ✅ 已修復

## 待辦清單

- [x] 建立 pyproject.toml（專案元數據）
- [x] 路徑配置外部化（config.py）
- [x] 將 scripts 改為 ideas2tasks/ 包結構
- [x] 加入 __init__.py 導出公共 API
- [x] 移除所有 sys.path.insert
- [ ] 初始化 uv 專案（.python-version, uv.lock）— 跳過
- [x] 刪除 executor.py.bak
- [x] 清理 __pycache__
- [x] 更新 SKILL.md 反映新結構

## 已完成工作

### 2026-04-23 17:58
- 建立正式 Python 包結構（ideas2tasks/）
- 實作 config.py 配置管理模組
- 遷移 scan.py、classify.py、state_sync.py
- 建立 __main__.py CLI 入口點
- 建立 pyproject.toml 專案元數據
- 更新 SKILL.md 文件
- 向後兼容舊 cron 任務（保留 scripts/）

### 驗證通過
```bash
# 配置模組測試
PYTHONPATH=/Users/claw/.qclaw/workspace/skills/ideas2tasks python3 \
  -c "from ideas2tasks.config import get_tasks_dir; print(get_tasks_dir())"
# 輸出：/Users/claw/Tasks

# 狀態同步測試
PYTHONPATH=/Users/claw/.qclaw/workspace/skills/ideas2tasks python3 \
  -c "from ideas2tasks.state_sync import get_tasks_dir_status; import json; print(json.dumps(get_tasks_dir_status('ideas2tasks'), indent=2))"
# 輸出：{"T001": "done", "T002": "done", ..., "T007": "pending"}
```

### Git 提交
- Commit: `5c5e8cc` — openclaw-skills
- 訊息：`refactor: ideas2tasks Python 环境规范重构 (T007)`

## 技術細節

### pyproject.toml 範例
```toml
[project]
name = "ideas2tasks"
version = "2.2.0"
description = "將 Ideas 目錄的想法轉為敏捷任務"
requires-python = ">=3.10"
dependencies = []  # 純標準庫

[project.scripts]
ideas2tasks = "ideas2tasks.__main__:main"
```

### 配置檔結構
```json
{
  "tasks_dir": "/Users/claw/Tasks",
  "ideas_dir": "/Users/claw/Ideas",
  "telegram_config": "~/.qclaw/gold_monitor_config.json"
}
```

### 包結構重組
```
ideas2tasks/
├── __init__.py
├── __main__.py      # CLI 入口點
├── cli.py           # 命令分派
├── scan.py
├── classify.py
├── state_sync.py
└── config.py       # 配置載入
```

## 驗收標準
- [x] python3 -m ideas2tasks 能正確執行
- [x] 無 sys.path.insert 殘留
- [x] pyproject.toml 存在且有效
- [x] 配置可從外部檔案覆寫路徑
- [ ] 所有現有 cron 任務仍正常運作 — 需用戶驗證

## 關聯任務
- 無

## 備註
此為技術債清理，不影響現有功能邏輯。
舊 scripts/ 目錄保留以向後兼容現有 cron 任務。

## 下一步
~~等待用戶驗證 cron 任務是否正常運作後，可將狀態改為 done。~~

✅ 用戶已確認（2026-04-23 18:01），cron 任務正常運作。T007 done。
